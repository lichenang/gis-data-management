## 1. API 层

- [x] 1.1 `api/image.ts` 新增 `ImageDownloadUrlResponse` 接口定义
- [x] 1.2 `api/image.ts` 新增 `getImageDownloadUrl(id)` 函数

## 2. 视图层

- [x] 2.1 `views/images/index.vue` 操作列新增"下载原始影像"按钮
- [x] 2.2 `views/images/index.vue` 新增 `handleDownload` 函数
